# CONTEXT.md — Từ điển domain

## Habitchecker

- **Việc (Task)** — một mục người dùng theo dõi. Hai loại: `repeat: 'daily'` (việc hằng ngày, có hiệu lực từ `createdAt`, kết thúc khi `archivedAt`) và `repeat: 'once'` (việc một lần, chỉ có hiệu lực đúng ngày `date`).
- **Ngày có hiệu lực (Active day)** — ngày mà một việc được tính: daily: `createdAt ≤ ngày` và (`archivedAt` trống hoặc `ngày < archivedAt`); once: `ngày === date`.
- **Tick (Record)** — đánh dấu đã làm một việc vào một ngày; lưu trong `records[ngày][taskId]`.
- **Ngưỡng (minDone)** — **ĐÃ BỎ** *(quyết định 12/07/2026)*: trước đây là số việc tối thiểu để một ngày tính hoàn thành. Đã thay bằng định nghĩa "xong hết việc bắt buộc" (xem bên dưới); trường `minDone` không còn trong `state` (normalize bỏ qua, dữ liệu cũ nhập vào vẫn chạy), mục "Cài đặt chuỗi" ở tab Công việc đã gỡ.
- **Ngày hoàn thành (Day complete)** — *(quyết định 12/07/2026, thay quyết định ngưỡng 08/07)*: ngày hoàn thành = tick đủ **MỌI việc bắt buộc** (`required`) có hiệu lực ngày đó — `requiredTotal > 0 && requiredMissing === 0`. Việc thường không ảnh hưởng chuỗi, chỉ hiện trong tiến độ/thống kê. **Ngày không có việc bắt buộc nào → không hoàn thành** (chuỗi không chạy khi chưa đánh dấu ★ việc nào — tránh chuỗi "tự lớn" vô nghĩa). Mẫu số (`total`) vẫn chỉ tính việc hằng ngày; việc một lần đã tick **"chỉ cộng, không trừ"** — cộng vào `done` (có thể vượt `total`, vd `5/3`) nhưng không thế chỗ được việc bắt buộc bỏ sót. Định nghĩa dùng thống nhất ở mọi nơi: chuỗi, heatmap Năm, hàng Tổng của Bảng.
- **Việc bắt buộc (Required)** — *(quyết định 12/07/2026)*: cờ `required` chỉ có nghĩa với **việc hằng ngày**, và từ nay là **tiêu chí duy nhất** của ngày hoàn thành. Cờ áp **hồi tố** cho mọi ngày việc đó có hiệu lực: bật/tắt cờ làm chuỗi/heatmap tính lại. Cùng phiên này đã **loại** đề xuất lưu giờ tick (`completedAt`): checkbox là trạng thái, không phải sự kiện thời gian.
- **Nhóm việc (Group)** — *(quyết định 12/07/2026)*: `state.groups = [{ id, name }]`, việc trỏ nhóm qua `groupId`; xóa nhóm không xóa việc (việc về "không nhóm"). **Thứ tự trong mảng `state.groups` là thứ tự hiển thị** — người dùng kéo xếp ở tab Công việc (`reorderGroups`). Tab Hôm nay hiển thị việc chia mục theo `groupTasks` (habit-model): mục ảo **★ Bắt buộc** tự gom mọi việc `required` và LUÔN đứng đầu (chỉ là cách hiển thị — `groupId` của việc giữ nguyên, bỏ cờ là việc quay về nhóm gốc); rồi tới các nhóm thật theo thứ tự; việc không nhóm (hoặc nhóm đã xóa) vào mục "Không nhóm" cuối. Mục rỗng ẩn; chưa dùng nhóm/bắt buộc thì danh sách phẳng như cũ, không tiêu đề.
- **Chuỗi (Streak)** — số ngày hoàn thành liên tiếp, đếm lùi từ hôm nay; hôm nay chưa đạt thì đếm lùi từ hôm qua.
- **Ngày nghỉ (Skip)** — *(quyết định 12/07/2026)*: `state.skips = { 'YYYY-MM-DD': 'lý do' }` (lý do có thể rỗng — kiểm tra bằng `in`, không đọc giá trị). Ngày nghỉ **trung lập** với mọi loại chuỗi (chuỗi chung lẫn chuỗi từng việc): không làm đứt, không cộng — kể cả hôm đó tick đủ. Không cứu được ngày trượt thật trước/sau nó. Đánh dấu lùi cho ngày quá khứ được (hàng Tổng của Bảng) — nhất quán với việc bảng cho tick lùi. Heatmap Năm: ô kẻ chéo riêng, loại khỏi mẫu số "ngày có việc" của tổng kết năm. `dayStats` vẫn đếm tick như thường và trả thêm cờ `skipped` — người gọi xét cờ trước khi diễn giải `complete`.
- **Mức đậm heatmap (Heat level)** — *(cập nhật 12/07/2026)*: mức 4 (đậm nhất) = **hoàn thành + trọn vẹn** (`complete` và `dailyDone === total` — việc một lần không thế chỗ được; kèm `complete` để ngày chưa có việc bắt buộc không hiện đậm nhất dù tick hết); mức 3 = hoàn thành (đủ việc bắt buộc, giữ chuỗi); mức 1–2 = chưa hoàn thành, theo tiến độ `done/total`; mức 0 = chưa tick gì.
- **Thành tựu (Achievement)** — ghi chú tự do theo ngày, độc lập với Việc/Tick. *(12/07/2026)*: UI đổi khung thành **"Nhật ký"** — chứa cả thành tựu lẫn ghi chú đời thường (mệt, mất ngủ...); tên `achievements` trong code/`state` giữ nguyên (tương thích dữ liệu cũ). Đã cân nhắc và **hoãn** việc thêm `kind: 'win' | 'note'` — chỉ làm nếu dùng thực tế thấy cần phân loại.
- **habit-model** — module domain thói quen (`Habitchecker/habit-model.js`, ES module, hàm thuần nhận `state` tường minh).
